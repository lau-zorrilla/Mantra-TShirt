<%= form_for(@shirt, :html => {:multipart => true }) do |f| %>
  <% if @shirt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shirt.errors.count, "error") %> prohibited this shirt from being saved:</h2>

      <ul>
      <% @shirt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" id="fName">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field" id="fDescription">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field" id="fImage">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions" id="fSubmit">
    <%= f.submit %>
  </div>
<% end %>
