<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @shirts.each do |shirt| %>
      <div class="cutout">
        <p id="name"><%= shirt.name %></p>
        <div class="blackLine"></div>
        <div><%= shirt.image %></div>
        <div class="blackLine"></div>
        <p id="description"><%= shirt.description %></p>
        <%= link_to 'Show', shirt %>
        <%= link_to 'Edit', edit_shirt_path(shirt) %>
        <%= link_to 'Destroy', shirt, method: :delete, data: { confirm: 'Are you sure?' } %>
     </div>

    <% end %>
  </tbody>
</table> 

<br>

<%= link_to 'New Shirt', new_shirt_path %>
