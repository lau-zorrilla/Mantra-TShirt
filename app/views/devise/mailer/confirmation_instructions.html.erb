<p><%= link_to t('welcome')%> <%= @email %>!</p>

<p><%= link_to t('confirm_account_email')%>:</p>

<p><%= link_to t('confirm_account'), confirmation_url(@resource, :confirmation_token => @token) %></p>
